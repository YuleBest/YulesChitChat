---
title: 强调（粗体、斜体和高亮） - 于乐的 Markdown 教程
shortTitle: 粗体、斜体和高亮
description: 介绍 Markdown 的强调语法
isOriginal: true
author: Yule
date: 2025-11-24

category: ["笔记"]
tag: ["Markdown", "教程"]
excerpt: Markdown 支持粗体、斜体和高亮

order: 8
---

Markdown 支持强调语法，包含了粗体、斜体和高亮。

## 粗体

粗体的语法是将要粗体的内容使用两个星号 `**TEXT**` 或两个下划线 `__TEXT__` 包围，推荐使用星号：

::: tabs

@tab Markdown

```md
Markdown 支持**粗体**语法
```

@tab 渲染效果

Markdown 支持**粗体**语法

:::

## 斜体

斜体的语法是将要斜体的内容使用一个星号 `*TEXT*` 或一个下划线 `_TEXT_` 包围，推荐使用星号：

::: tabs

@tab Markdown

```md
Markdown 支持*斜体*语法
```

@tab 渲染效果

Markdown 支持*斜体*语法

:::

## 高亮

高亮的语法是将要高亮的内容使用两个等于号 `==TEXT==` 包围：

::: tabs

@tab Markdown

```md
Markdown 支持==高亮==语法
```

@tab 渲染效果

Markdown 支持==高亮==语法

:::

## 特性

1. 强调语法不能跨行或跨段使用
2. 代码、代码块中不支持强调语法
3. 引言中支持强调语法
